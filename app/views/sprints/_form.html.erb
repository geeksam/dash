  <% if @sprint.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sprint.errors.count, "error") %> prohibited this sprint from being saved:</h2>

      <ul>
      <% @sprint.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :team_id %><br />
    <%= f.select :team_id, Team.all.map { |e| [e.name, e.id] } %>
  </div>
  <div class="field">
    <%= f.label :number %><br />
    <%= f.text_field :number %>
  </div>
  <div class="field">
    <%= f.label :starts_on %><br />
    <%= f.date_select :starts_on %>
  </div>
  <div class="field">
    <%= f.label :ends_on %><br />
    <%= f.date_select :ends_on %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
